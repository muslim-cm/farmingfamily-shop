[build]
  publish = "frontend"

# This ensures root URLs work
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

# This handles direct file access
[[redirects]]
  from = "/*"
  to = "/:splat"
  status = 200