[build]
  publish = "frontend"

# Redirect root to index.html
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

# Handle all other paths - keep them as-is
[[redirects]]
  from = "/*"
  to = "/:splat"
  status = 200