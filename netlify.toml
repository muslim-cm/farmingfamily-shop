[build]
  publish = "frontend"

# This ensures /frontend/ paths work
[[redirects]]
  from = "/frontend/*"
  to = "/:splat"
  status = 200

# Root redirect
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200